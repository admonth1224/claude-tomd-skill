# tomd skill — Python dependencies
# Office/PDF/CSV/HTML/이미지 변환 (docx, xlsx, pptx, pdf 등 한 번에)
markitdown[all]>=0.1.5
# HWP(한글 .hwp 바이너리) 텍스트 추출
pyhwp>=0.1b15
# (.hwpx, .md, .txt 는 표준 라이브러리만으로 처리 — 추가 의존성 없음)
